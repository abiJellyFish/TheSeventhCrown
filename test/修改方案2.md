# 修改方案 2 — UI 布局修复、快捷键可用性、时间系统、窗口自适应

---

## 问题清单

1. **快捷键标签误导** — LeftPanel 列出 `[1]探查 [2]躲藏 ... [S]法术` 等 15 个快捷键，但大部分 handler 只输出"此功能待开发"，按键后无实际效果
2. **日志刷新不更新** — `ActionLog.add()` 调 `self.refresh()` 与 `refresh_all()` 调 `refresh_content()` 两条路径冲突，导致日志面板不刷新
3. **UI 边框不紧凑** — 相邻 widget 各自 `border: solid`，形成双边框间隙
4. **TopBar 信息缺失** — 仅显示 `地名 晴 午间` 硬编码，缺少游戏内时间和先攻顺位；时间计算用错误常量
5. **窗口拖动时遮挡/显示不全** — 纯 `fr` 布局无最小尺寸约束，小窗口时各区域被压缩

---

## 一、快捷键：标签与实际功能对齐

### 现状

LeftPanel 当前显示 20 个快捷键，但只有 5 个有实际功能：

| 显示标签 | 按键 | 实际行为 | 状态 |
|---------|------|---------|------|
| `[0]交互` | 0 | `action_interact()` 正常 | 可用 |
| `[Tab]交互` | Tab | `action_interact()` 正常 | 可用 |
| `[r]短休` | r | `short_rest()` 正常 | 可用 |
| `[X]观察` | x | `toggle_observe()` 正常 | 可用 |
| `[Q]退出` | q | `quit()` 正常 | 可用 |
| `[R]长休` | R | `long_rest()` 函数已存在，未接入 | 简单接入 |
| `[g]慢速` | g | "此功能待开发" | 待实现 |
| `[G]疾走` | G | "此功能待开发" | 待实现 |
| `[,]消磨` | , | "此功能待开发" | 待实现 |
| `[1]探查` | 1 | "此功能待开发" | 待实现 |
| 其余 10 个 | 2-8,/,A,S | "此功能待开发" | 待实现 |

### 方案

**原则：LeftPanel 只展示已有实际功能的快捷键，未实现的移入 dim 标注区。**

重写 `LeftPanel.render()`：

```python
def render(self) -> str:
    lines = []
    if self.state and self.state.in_combat:
        p = self.state.player
        filled = int(p.ap / max(p.max_ap, 1) * 10)
        lines.append(f"AP [{'|'*filled}{'.'*(10-filled)}]")
        lines.append("S-Tab 结束回合")
    # 可用快捷键
    lines.extend([
        "[0]交互 [1]探查  [r]短休 [R]长休",
        "[g]慢速 [G]疾走  [,]消磨 [X]观察",
        "[Tab]交互 [Q]退出",
    ])
    # 未实现 — dim 标注，不误导
    lines.append("")
    lines.append("[dim]待实现: 2躲藏 3协助 4跳跃[/]")
    lines.append("[dim]5撤离 6回避 7推撞 8擒抱[/]")
    lines.append("[dim]/击晕 A动作 S法术[/]")
    return "\n".join(lines)
```

**接入已有但未使用的功能：**

| 按键 | 实现方式 |
|------|---------|
| `R` 长休 | `core/rest.py` 已有 `long_rest()`，app.py import 加上即可 |
| `1` 探查 | 复用观察模式逻辑，搜索相邻 8 格，输出发现摘要 |
| `g` 慢速 | 设置 `state.slow_mode = True`，移动时 clock tick 翻倍 |
| `G` 疾走 | 同方向连走 2 格（复用 `_move_player` 两次） |
| `,` 消磨 | `clock.tick_action(1.0)` 推进时间，日志输出"时间流逝..." |

---

## 二、日志刷新机制修复

### 根因

两条刷新路径冲突：

| 路径 | 调用链 |
|------|--------|
| A | `ActionLog.add()` → `self.refresh()` |
| B | `refresh_all()` → `w.refresh_content()` → `self.update(self.render())` |

`update()` 直接设置渲染内容，绕过 Textual 标准刷新周期。路径 B 执行后路径 A 的 `refresh()` 标记被覆盖，导致消息已添加但界面不更新。

### 方案

**删除 `refresh_content()`，统一用 Textual 标准 `self.refresh()`：**

```python
# 删除所有 Widget 的 refresh_content() 方法

# refresh_all() 改为：
def refresh_all(self) -> None:
    for w in [self._map_view, self._left_panel, self._right_panel,
              self._top_bar, self._act_log, self._scene_log]:
        if w:
            w.refresh()
```

`Static.render()` 会被 `refresh()` 标记后自动调用，无需手动 `update()`。

---

## 三、UI 边框紧凑

### 根因

两个 `border: solid` 的 widget 相邻时，相邻边各有一条边框线，形成双线间隙：

```
当前:  [widget A]||[widget B]    ← 双边框
期望:  [widget A]|[widget B]     ← 单边框
```

### 方案

**外层容器统一边框，子 widget 用单边 border：**

```css
* { margin: 0; padding: 0; overflow: hidden; }

#top { height: 1; border: solid #444444; padding: 0 1; }

#main { height: 4fr; min-height: 15; }
#left { width: 2fr; min-width: 14; border-right: solid #444444; padding: 0 1; }
MapView { width: 3fr; min-width: 20; content-align: left top; }
#right { width: 2fr; min-width: 18; border-left: solid #444444; padding: 0 1; }

#input-bar { height: 1; border: solid #444444; }

#log-area { height: 1fr; min-height: 6; border: solid #444444; }
#action-log { width: 1fr; min-width: 20; border-right: solid #444444; padding: 0 1; }
#scene-log { width: 1fr; min-width: 20; padding: 0 1; }
```

- 容器间重叠边框：`#main` 底边 + `#input-bar` 顶边 + `#log-area` 顶边 → 通过相邻容器用 `border-top: none` 或直接保留单线（视觉上可接受）
- 可以用 `Separator` widget 替代 border 做水平分隔线，但当前方案已足够紧凑

---

## 四、TopBar 内容补全 + 时间系统修正

### 时间常量

游戏时间由玩家按键推进的钟摆数决定：

```
5000 钟摆 = 1 天
50000 钟摆 = 1 月（10 天）
250000 钟摆 = 1 年（5 月 = 50 天）
```

时间显示格式：`第D天  M月  Y年纪年  (N钟摆)`

```python
PENDULUMS_PER_DAY = 5000
PENDULUMS_PER_MONTH = 50000     # 10 天
PENDULUMS_PER_YEAR = 250000     # 5 月

pc = clock.pendulum_count
day = (pc // PENDULUMS_PER_DAY) % 10 + 1
month = (pc // PENDULUMS_PER_MONTH) % 5 + 1
year = pc // PENDULUMS_PER_YEAR + 1
```

### 先攻顺位

战斗中从 `state.combat_initiative` 提取，当前回合者高亮：

```python
if s.in_combat and s.combat_initiative:
    names = []
    for e in s.combat_initiative:
        if e.hp <= 0:
            continue
        nm = e.name
        if e is s.combat_turn_entity:
            nm = f"[bold yellow]{nm}[/]"
        names.append(nm)
    init_text = " > ".join(names)
else:
    init_text = ""
```

### TopBar 三段式布局

```
[地名  天气]              [先攻顺位]              [第D天 M月 Y纪年 N钟摆]
```

```python
def render(self) -> str:
    s = self.state
    if s is None: return ""
    clock = s.clock
    PPD = 5000
    PPM = 50000
    PPY = 250000
    pc = clock.pendulum_count
    day = (pc // PPD) % 10 + 1
    month = (pc // PPM) % 5 + 1
    year = pc // PPY + 1

    left = f"[bold]{s.current_map or '???'}[/]  晴"

    if s.in_combat and s.combat_initiative:
        names = []
        for e in s.combat_initiative:
            if e.hp <= 0: continue
            nm = e.name
            if e is s.combat_turn_entity: nm = f"[bold yellow]{nm}[/]"
            names.append(nm)
        center = " > ".join(names)
    else:
        center = ""

    right = f"第{day}天 {month}月 {year}纪年 {pc}钟摆"
    return f" {left}   {center}   {right}"
```

---

## 五、窗口大小自适应

### 方案

添加最小尺寸约束 + 溢出隐藏：

- `min-height`/`min-width` 防止区域被压缩到不可用
- `overflow: hidden` 全局防止内容溢出
- 总最小宽度 ≈ 14 + 20 + 18 = 52 列（勉强可用的最小终端尺寸）

完整 CSS 见第三节。

---

## 六、交互无反馈兜底

`action_interact()` 末尾加空地兜底：

```python
# 在方法末尾，所有判断分支之后：
self._act_log.add("凯恩 环顾四周，这里没什么特别的")
```

---

## 七、实施步骤

### 步骤 1: 修复日志刷新机制

**文件:** `render/textual/app.py`

- 删除 `TopBar.refresh_content()`、`LeftPanel.refresh_content()`、`MapView.refresh_content()`、`RightPanel.refresh_content()`、`ActionLog.refresh_content()`、`SceneLog.refresh_content()`
- `refresh_all()` 改为 `w.refresh()`
- 确认 `ActionLog.add()` 和 `SceneLog.set_scene()` 中的 `self.refresh()` 调用保留

### 步骤 2: 边框紧凑化 + CSS 重写 + 窗口自适应

**文件:** `render/textual/app.py`

- 重写 `CSS` 类变量：单边 border + min-width/min-height + overflow
- 验证小窗口下不遮挡

### 步骤 3: TopBar 内容补全 + 时间系统

**文件:** `render/textual/app.py`

- 重写 `TopBar.render()`，计算游戏内时间（5000 钟摆/天）
- 战斗中显示先攻顺位
- 时间格式：`HH:MM 第D天 M月 Y纪年`

### 步骤 4: 快捷键对齐 — 接入已有功能

**文件:** `render/textual/app.py`

- `R` 长休：import `long_rest`，实现 `action_long_rest()`
- `1` 探查：搜索相邻 8 格，输出生物/物品摘要
- `g` 慢速：GameState 加 `slow_mode` 字段，移动时 clock tick 翻倍
- `G` 疾走：同方向连走 2 格
- `,` 消磨：推进 30 钟摆，日志输出

### 步骤 5: LeftPanel 重写 — 只展示可用快捷键

**文件:** `render/textual/app.py`

- 可用快捷键清晰列出
- 未实现功能用 `[dim]...[/]` 标注，与可用项区分

### 步骤 6: README 更新

**文件:** `README.md`

- 更新界面布局 ASCII 图
- 更新快捷键表格，标注哪些可用、哪些待实现
- 更新时间系统说明

### 步骤 7: 更新修改日志

**文件:** `修改日志.md`
